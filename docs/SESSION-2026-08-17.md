# Session log, 2026-08-16 evening to 2026-08-17 (UTC)

Goal set at 05:00: GLM-5.2 on four Sparks, on AEON's stack, with DSpark block drafting,
NVFP4 CUTLASS MoE, and a cross-node capture recipe handed back to AEON; plus two boot-time wins
(persistent JIT cache, fail-closed writer gate). Rules I held to: production restored at the end of
every experiment chain; new code boots at 32K first; no bulk transfers on a node during a boot.

## Fleet state going in
Production up 04:57 after the power cycle (KV pool 989,606 @316K), head CPU cap cleared by
the cold cycle, mgmt NICs and rails persistent, watchdog load-only, earlyoom off, launcher
single-flight + in-progress guard + evidence capture, `--disable-custom-all-reduce` and
persisted b12x/CuTe JIT cache in every launcher.

## Results (same probes: 2K C4 / 2K C16 / 14K C4, correctness gate, acceptance from /metrics)

| config | boot | gate | 2K C4 e2e / per-req / prefill | 14K C4 e2e / prefill | accepted/step | note |
|---|---|---|---|---|---|---|
| A. MRv2 + 0xdfi adaptive MTP 2/4/5 + decode-aware prefill @32K s4 | up (3rd try; first two = my page-cache mistake) | PASS x4/x8 | 34.30 / 11.05 / 359 | engine died mid-probe (uncaptured) | 1.63-1.85 (2.6-2.85 tok/step) | prefill throttled by decode-aware scheduling; MRv2 + FULL cross-node graphs work on our fleet |
| BASELINE. production V1 MTP k=2 @32K s4 | up 1st try, 8 min to KV (no 300 s stall) | PASS | 35.93 / 11.19 / 566 | 13.85 / 559 | 1.146 (2.15 tok/step, 57% draft accept) | shape-matched reference; A's +25-30% tokens/step from adaptive did not change per-request decode |
| BASELINE-DCP1. production V1 MTP k=2, DCP1 @32K s4 | up 1st try | PASS | 43.30 / 12.45 / 956 | 18.55 / 825 | 1.152 (2.15 tok/step) | fair pair for C: stock DSpark at DCP1 is 21% slower e2e than MTP k=2 with the same drafter bird gets 2.1 from |
| C. DSpark, bird quanttrio-ft drafter (bf16), k=3, MRv2, **DCP1** @32K s4 | up after 4 fixes (draft quant config, per-draft KV dtype, DCP1 because TRITON_ATTN drafter cannot do DCP, no a2a at DCP1) | PASS | 34.13 / 10.16 / 523 | 12.40 / 746 | 0.755 (1.76 tok/step, 25% draft accept) | runs and is correct; acceptance far below bird's ~2.1 with the same drafter -> the ring-buffer draft window + cache-hit trim from his branch is the missing port (and possibly aux-layer indexing in 0.27.0); no speed win yet; docs/DSPARK.md |
| D. B12X indexer port DCP>1 (our _merge_b12x_dcp_topk), DCP-sharded scratch @32K s4 DCP4 | up 1st try (the 316K version of this boot is what OOM'd the fleet at 00:09; sharded scratch fixed it) | PASS x4/x8 | 36.97 / 11.40 / 584 (+3/+2/+3% vs DCP4 baseline) | 14.49 / 602 (+4.6% e2e, +7.6% prefill) | MTP k=2 | first live proof of the DCP>1 B12X indexer; his public code raises above DCP1. Pool 741K vs 1.11M (indexer scratch). Candidate for production after a 316K boot |
| D2. same indexer port at production shape 316K s16 (logits cap 256 MB) | up 1st try | PASS x4/x8 | 35.87 / 11.59 / 561 (prod 37.79 / 11.76 / 633) | 14.44 / 594 (prod 14.87 / 618) | MTP k=2 | **slower** 3-11% and pool 704,595 vs ~975K (-27%): the paged-indexer scratch scales with context x batch; the fallback path holds up better at depth. Proven, correct, NOT adopted for production. |
| B. production + fuse_allreduce_rms + fuse_gemm_comms @316K s16 | up 1st try, 8 min | PASS | 37.24 / 11.42 / 618 (prod this morning: 37.79 / 11.76 / 633) | 14.77 / 615 (prod: 14.87 / 618) | MTP k=2 | **no-op** on our stack (all cells inside noise); bird's +1 tok/s was on his June tree at C1. Not adopted. |
| Platform: AEON v0.27.1 base + b12x@334a2d75 + our overlays baked (glm52-spark-platform:aeon-0.27.1), production V1 config @32K s4 DCP4 | **up 1st try** (his vLLM in site-packages; 6 of 33 overlay targets 3-way merged/kept, see platform/README.md); PIECEWISE + FULL-decode graphs captured cross-node on his image | PASS x4/x8 | 35.80 / 10.80 / 529 (stock: 35.93 / 11.19 / 566) | 14.04 / 574 (stock 13.85 / 559) | MTP k=2 | **parity** with stock; the platform works, and it carries his DSpark/NVFP4 machinery for the next steps. Pool 1,075,968 |
| NVFP4 (nvidia/GLM-5.2-NVFP4, 433 GiB) | on Storage (done 07:10). Not on nodes: needs QuantTrio staged out (916 GB disks). sshfs test path measured 10.7 MB/s = not viable. Launcher ready: launchers/champion_nvfp4weights_32k.sh on the platform image (CUTLASS NVFP4 MoE for sm_121a is AEON's build) | | | | | decision needed: stage QuantTrio out (~2 h round trip) |

## Staging (permanent rule now)
- DeepSeek-V4-Flash-0731 (156 GB): backing up to /Volumes/Storage/weights-staging/... with RESTORE.md; delete from nodes after verify.
- nvidia/GLM-5.2-NVFP4: downloading to Storage.
- DeepSeek backed up (58 files, rsync-verified zero diff), deleted from all four nodes: free now 260 / 314 / 300 / 308 GB.
- nvidia/GLM-5.2-NVFP4 (433 GiB) fully on Storage. Still does not fit on a node next to QuantTrio (378 GB) with images; NFS is TCC-blocked on the Mac, sshfs measured 10.7 MB/s (not viable). Path = stage QuantTrio out.
- Stale docker tags: deep prune DONE 09:45 (glm52-dcp:* x8, anemll, r0b0tlab, upstream vllm-openai base gone; representative glm52-dcp e232d26-adaptive-jul2 saved to /Volumes/Storage/images first). Nodes now carry 4-5 images each; free 286-301 GB per node.

## Final state 09:39
Production RESTORED (chain8) on the same V1 MTP k=2 DCP4 config with tonight's launcher additions (sparse_utils #50365 backport, preempt-guard scheduler, NCCL file logging, VLLM_BUILDA_BMM=0, no custom all-reduce, persisted JIT caches): boot 8 min, gate PASS, KV pool 983,978 @316K, battery 1K C4 36.12 e2e / 11.09 per-req / 603 prefill, 1K C16 61.92, 14K C4 14.67 / 612 (this morning: 37.79 / 11.76 / 633; 14.87 / 618). Inside noise. Fleet is yours.

## Recon that changed the plan
- 0xdfi runs MRv2 (VLLM_USE_V2_MODEL_RUNNER=1); we ran V1. His overlays apply to 0.27.0.
- bird (b1rd) already runs GLM-5.2 QuantTrio DSpark on 4x GB10 (July): ~24.7 tok/s C1 at 1M via ring drafting on his vLLM branch; his quant-finetuned drafter is what C tests. His training pipeline is in the kit.
- RedHatAI/GLM-5.2-speculator.dspark exists (B200-validated); nvidia/GLM-5.2-NVFP4 exists (no requant needed).
- vLLM #46253 (AEON's capture blocker) is capture_error_mode + rank drift; our 0.27.0 captures cross-node without a code fix, so the contribution back is a validated recipe on his base, not a patch claim.
- Indexer pattern: 0.27.0 derives FFFSSS... from the checkpoint's index_topk_freq/offset; no override needed.

## Also done tonight
- `ncclras` built on all four; `wedge_capture.sh` proven both ways on live production (RAS JSON `collective_counts` deltas + running-request context). #51921 reply posted . NCCL persistent logging, preempt-guard scheduler, `VLLM_BUILDA_BMM=0` re-applied to production launcher (they were caught in the 01:58 blanket revert).
- `--disable-custom-all-reduce` in every launcher: the 300 s init stall is gone; production-shape boots are now ~8 min.
- b12x CuTe-DSL kernel cache persisted (`B12X_CUTE_COMPILE_CACHE_DIR`).
- Writer build gate (`gates/nvfp4_writer_gate.py`) added to the kit (fail-closed like AEON's nvfp4_kv_gate); runs in chain8's gap.
- Kit repo commits: platform Dockerfile, DSpark doc + bird's ring-buffer patch series with port notes, DSpark training pipeline (bird, provenance), spec acceptance probe, weight_utils tolerance overlay, wedge_capture. spark-fleet-guard repo has 2 commits. Nothing pushed.
- Head-side watchdog: could not be reloaded from a remote session (launchd bootstrap needs a local login); the node-side sentinel + guarded wrapper cover recovery meanwhile.

## Not done at the end of the night
- NVFP4 boot: needs QuantTrio staged out (production swap) or another disk. Launcher and platform image are ready.
- DSpark at DCP4 and at bird's acceptance: the ring-buffer port (patch series + notes in kit). Multi-hour, code work, no fleet needed until validation.
- Training our own DSpark head: needs the capture hook (part of the same port).
- Publishing: held until the results were re-checked; the repos went public on 2026-08-17 evening.

## Day session (10:00-16:45 UTC): DSpark ring port and the acceptance investigation
- bird's ring-buffer draft KV series re-derived onto 0.27.0 MRv2 (~400 lines net; overlays/dspark-ring). Ring DSpark boots
  first try and passes the gate at DCP1 and DCP4 (first DSpark at DCP>1 on this stack; draft owns no allocator KV, 32K pool
  1.43-1.52M vs 1.11M for MTP). Capture hook live; DDP finetune launcher adapted to the fleet; RedHat base drafter staged on
  Storage + nodes.
- Acceptance is the verdict: ring DSpark k=3 accepts ~1.0/step greedy (0.68 on natural docs) vs MTP k=2 1.28 (1.42 on the same
  docs). Ruled out by experiment: temperature, target KV dtype (fp8 vs nvfp4), drafter revision. Two ring asymmetries vs the
  paged path fixed (+15%). Offline teacher-forced accuracy of bird's ft drafter on OUR captured hidden states: p1 0.575 (an
  earlier 0.69 was on repeated-sentence filler and is void). Conclusion: on this target/quant the drafter is not accurate enough
  to beat MTP k=2; bird's 24.7 vs 13.7 was against no speculation. Route to make it pay = finetune on our own captures (ready,
  needs ~75 min of GPUs) and it must clear ~1.6/step. Full matrix in kit docs/DSPARK.md.
- Production restored after every chain (last: 16:31, gate PASS, pool 997,026, 1K C4 37.72 / 11.61 / 605).
- NVFP4 boot happened later the same day after staging QuantTrio out (see the AEON hand-back repo, NVFP4.md): boots and serves with the CUTLASS FP4 MoE at gmu 0.92, 107 GB/rank, not the decode path; MTP on top does not fit and thrashes the nodes.
